import React from "react";
import styles from "../page.module.css";

const AIGridLayout = ({ children }) => {
  return <div className={styles.aiGridLayout}>{children}</div>;
};

export default AIGridLayout;
